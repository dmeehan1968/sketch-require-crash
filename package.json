{
  "name": "skpm-require-crash",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "skpm-require-crash",
    "manifest": "src/manifest.json",
    "main": "plugin.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {},
  "author": "Dave Meehan\n <dave_meehan@replicated.co.uk\n>",
  "dependencies": {
    "@skpm/builder": "^0.4.1"
  }
}
